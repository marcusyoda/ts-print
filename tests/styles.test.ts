import {expect} from 'chai';

describe('Colors', function() {
  it('Must has INFO color', function() {
    const result = (5 + 2);
    expect(result).equal(7);
  });
  it('Must has NOTICE color', function() {
    const result = (5 + 2);
    expect(result).equal(7);
  });
  it('Must has WARN color', function() {
    const result = (5 + 2);
    expect(result).equal(7);
  });
  it('Must has ERR color', function() {
    const result = (5 + 2);
    expect(result).equal(7);
  });
  it('Must has FAIL color', function() {
    const result = (5 + 2);
    expect(result).equal(7);
  });
  it('Must has SUCCESS color', function() {
    const result = (5 + 2);
    expect(result).equal(7);
  });
  it('Must has OK color', function() {
    const result = (5 + 2);
    expect(result).equal(7);
  });
});
